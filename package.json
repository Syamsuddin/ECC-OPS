{
  "name": "logen",
  "version": "1.0.1",
  "private": true,
  "description": "LOGEN — Linux Operational Agent. Hook & tooling test harness for the Claude Code plugin.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "validate": "bash tools/validate.sh --strict"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
